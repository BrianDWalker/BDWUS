import React, { useMemo, useState } from "react";
import { PageHeader } from "./Shell";
import { Icon } from "./Icons";
import { DataTable, Panel, StatusTag, formatMoney } from "./Primitives";
import { customers, leads, opportunities, quotes } from "../data/mockData";

const owners = ["Sarah Johnson", "Tia Brooks", "Sam Malik", "Ari Fox", "Maya Ortiz"];
const stages = ["New", "Discovery", "Solutioning", "Quote", "Approval", "Closed Won", "Closed Lost"];
const salesTabs = ["Leads", "Opportunities", "Accounts", "Custom Pricing", "Approvals", "Contracts", "Sales Enablement"];
const opportunityTabs = ["Summary", "Serviceability", "Quote Build", "Pricing", "Approvals", "Activities", "Contract", "Order Handoff"];
const quoteTabs = ["Quote Summary", "Line Items", "Pricing Waterfall", "Approvals", "PDF Preview", "Audit"];
const leadTabs = ["Qualification", "Account Fit", "Conversion Plan", "Activity"];

const sum = (items, selector) => items.reduce((total, item) => total + selector(item), 0);
const textMatch = (value, query) => String(value ?? "").toLowerCase().includes(query.trim().toLowerCase());
const billingAccountNumber = customer => `BA-${customer.id.replace("CUST-", "")}-01`;
const matchAny = (item, query, fields) => !query.trim() || fields.some(field => textMatch(field(item), query));
const customerFor = id => customers.find(customer => customer.id === id) || customers[0];

function opportunityMeta(opportunity) {
  const customer = customerFor(opportunity.customerId);
  const index = opportunities.findIndex(item => item.id === opportunity.id);
  const estimatedMrc = Math.round(opportunity.value / 36);
  const estimatedNrc = 12500 + index * 4300;
  const margin = [39.8, 22.5, 35.7, 28.4][index % 4];
  const stage = stages[(index + 1) % 5];
  return {
    ...opportunity,
    account: customer.name,
    accountNumber: customer.id,
    billingAccount: billingAccountNumber(customer),
    market: customer.region,
    segment: customer.segment,
    locations: [12, 34, 38, 8][index % 4],
    serviceMix: customer.services.join(", "),
    serviceability: ["On-net", "Wireless footprint", "Mixed on-net / near-net", "Requires engineering"][index % 4],
    estimatedMrc,
    estimatedNrc,
    tcv: opportunity.value + estimatedNrc,
    margin,
    pricingRisk: margin < 25 ? "High" : margin < 32 ? "Medium" : "Low",
    owner: owners[index % owners.length],
    nextStep: ["Discovery call", "Pricing review", "Quote approval", "Customer follow-up"][index % 4],
    stage,
    status: stage === "Approval" ? "Approval Required" : stage,
    solutionEngineer: owners[(index + 2) % owners.length]
  };
}

function quoteMeta(quote) {
  const customer = customerFor(quote.customerId);
  const opportunity = opportunities.find(item => item.id === quote.opportunityId) || opportunities[0];
  const opp = opportunityMeta(opportunity);
  const index = quotes.findIndex(item => item.id === quote.id);
  const mrc = Math.round(quote.value / 36);
  const nrc = 8200 + index * 2600;
  const discount = quote.customPrice ? 12 + index * 3 : 4;
  const approvalRequired = quote.margin < 30 || discount > 10;
  return {
    ...quote,
    account: customer.name,
    accountNumber: customer.id,
    billingAccount: billingAccountNumber(customer),
    opportunityName: opportunity.name,
    opportunityId: opportunity.id,
    serviceability: opp.serviceability,
    locations: opp.locations,
    productPackage: quote.package,
    term: [36, 24, 48][index % 3],
    mrc,
    nrc,
    taxes: Math.round((mrc + nrc) * 0.084),
    discount,
    expiration: ["2026-06-15", "2026-06-30", "2026-05-31"][index % 3],
    owner: owners[(index + 1) % owners.length],
    tcv: quote.value + nrc,
    approvalRequired,
    approvalStatus: approvalRequired ? "Approval Required" : "Ready to Send",
    pricingRisk: quote.margin < 25 ? "High" : quote.margin < 31 ? "Medium" : "Low"
  };
}

function serviceLinesFor(opportunity) {
  return [
    { id: `${opportunity.id}-L1`, product: "Fiber / DIA Access", category: "Wireline", billingCode: "DIA-MRC", mrc: Math.round(opportunity.estimatedMrc * 0.58), nrc: Math.round(opportunity.estimatedNrc * 0.48), cost: Math.round(opportunity.estimatedMrc * 0.34), margin: 41, serviceability: opportunity.serviceability },
    { id: `${opportunity.id}-L2`, product: "Managed Router / CPE", category: "Managed", billingCode: "CPE-MRC", mrc: Math.round(opportunity.estimatedMrc * 0.18), nrc: Math.round(opportunity.estimatedNrc * 0.22), cost: Math.round(opportunity.estimatedMrc * 0.12), margin: 33, serviceability: "Install required" },
    { id: `${opportunity.id}-L3`, product: "Wireless Backup", category: "Wireless", billingCode: "WLS-BACKUP", mrc: Math.round(opportunity.estimatedMrc * 0.14), nrc: Math.round(opportunity.estimatedNrc * 0.12), cost: Math.round(opportunity.estimatedMrc * 0.08), margin: 43, serviceability: "Coverage check" }
  ];
}

function activityRows(id) {
  return [
    { id: `${id}-A1`, date: "2026-05-12", type: "Discovery", owner: "Sarah Johnson", note: "Confirmed service mix, locations, contract timing, and decision process." },
    { id: `${id}-A2`, date: "2026-05-13", type: "Serviceability", owner: "Network Ops", note: "Wireline footprint and wireless backup review completed." },
    { id: `${id}-A3`, date: "2026-05-14", type: "Pricing", owner: "Pricing Desk", note: "Custom pricing package prepared with margin guardrails." }
  ];
}

function SearchBox({ value, onChange, placeholder }) {
  return <label className="inline-search"><Icon name="search" className="button-icon" /><input value={value} onChange={event => onChange(event.target.value)} placeholder={placeholder} /></label>;
}
function MiniStat({ label, value, note }) { return <div className="mini-stat"><span>{label}</span><strong>{value}</strong>{note && <small>{note}</small>}</div>; }
function SummaryStrip({ items }) { return <section className="summary-strip">{items.map(item => <MiniStat key={item.label} {...item} />)}</section>; }
function Tabs({ tabs, active, onChange }) { return <div className="record-tabs" role="tablist">{tabs.map(tab => <button key={tab} type="button" className={tab === active ? "active" : ""} onClick={() => onChange(tab)}>{tab}</button>)}</div>; }
function ActionButton({ children, onClick, variant = "ghost-button", icon = "workflow" }) { return <button className={variant} type="button" onClick={onClick}><Icon name={icon} className="button-icon" />{children}</button>; }
function Modal({ title, subtitle = "Telecom sales workflow", children, actions, onClose }) { return <div className="sales-crm-modal-backdrop"><section className="sales-crm-modal" role="dialog" aria-modal="true"><header><div><strong>{title}</strong><span>{subtitle}</span></div><button type="button" className="sales-crm-close" onClick={onClose}>×</button></header><div className="sales-crm-modal-body">{children}</div><footer>{actions}</footer></section></div>; }

function SalesModalForm({ type }) {
  if (type === "lead") return <form className="sales-crm-form"><label>Lead Account<input placeholder="Prospect account name" /></label><label>Contact Name<input placeholder="Primary contact" /></label><label>Source<select><option>Website</option><option>Partner referral</option><option>Outbound</option><option>Customer referral</option></select></label><label>Product Interest<select><option>Fiber / DIA</option><option>Wireless</option><option>SD-WAN</option><option>Voice / UCaaS</option><option>Transitional Services</option></select></label><label>Estimated Value<input placeholder="$75,000" /></label><label>Owner<select>{owners.map(owner => <option key={owner}>{owner}</option>)}</select></label><label className="full">Qualification Notes<textarea placeholder="Locations, connectivity need, timeframe, decision maker, current provider..." /></label></form>;
  return <form className="sales-crm-form"><label>Account Number<input defaultValue="CUST-1001" /></label><label>Opportunity Name<input placeholder="Multi-site fiber and wireless expansion" /></label><label>Service Family<select><option>Wireline / Fiber</option><option>Wireless / Mobility</option><option>Transitional Services</option><option>Managed Network</option><option>Voice / UCaaS</option></select></label><label>Locations<input placeholder="12" /></label><label>Estimated MRC<input placeholder="$12,000" /></label><label>Estimated NRC<input placeholder="$12,500" /></label><label>Contract Term<select><option>36 months</option><option>24 months</option><option>12 months</option><option>Month to month</option></select></label><label>Sales Stage<select><option>Discovery</option><option>Solutioning</option><option>Quote</option><option>Custom Pricing</option><option>Approval</option></select></label><label className="full">Customer Need<textarea placeholder="Bandwidth expansion, wireless backup, managed router, installation constraints..." /></label></form>;
}

function SalesLandingTop({ query, setQuery, openModal }) {
  return <section className="sales-crm-simple-landing"><div className="sales-crm-landing-copy"><span>Telecom Sales Workspace</span><h2>Find the account, create the lead, or open an opportunity.</h2><p>A cleaner starting point for sales reps, managers, and pricers without putting every workflow on the first screen.</p></div><div className="sales-crm-landing-actions"><SearchBox value={query} onChange={setQuery} placeholder="Search account, opportunity, quote, product, stage" /><ActionButton icon="sales" variant="button" onClick={() => openModal("lead")}>Create Lead</ActionButton><ActionButton icon="opportunities" variant="button" onClick={() => openModal("opportunity")}>Create Opportunity</ActionButton></div></section>;
}

function WorkQueue({ opportunities: opps, quotes: quoteRows, setRoute }) {
  const workItems = [
    ...opps.slice(0, 3).map(item => ({ id: item.id, type: "Opportunity", account: item.account, owner: item.owner, priority: item.pricingRisk === "High" ? "High" : "Normal", nextAction: item.nextStep, route: `details/opportunity/${item.id}` })),
    ...quoteRows.filter(item => item.approvalRequired).slice(0, 2).map(item => ({ id: item.id, type: "Pricing Review", account: item.account, owner: item.owner, priority: item.pricingRisk, nextAction: "Review margin and approval", route: `details/quote/${item.id}` }))
  ];
  return <Panel title="My Work Queue" description="A focused queue for opportunities, approvals, pricing reviews, and serviceability requests."><DataTable columns={[{ key: "type", label: "Work Type" }, { key: "id", label: "Record" }, { key: "account", label: "Account" }, { key: "priority", label: "Priority", render: row => <StatusTag tone={row.priority === "High" ? "warn" : "blue"}>{row.priority}</StatusTag> }, { key: "nextAction", label: "Next Action" }, { key: "owner", label: "Owner" }, { key: "actions", label: "", render: row => <button className="link-button compact-action" type="button" onClick={() => setRoute(row.route)}>Open</button> }]} rows={workItems} /></Panel>;
}

export function SalesModule({ setRoute, showToast }) {
  const [modal, setModal] = useState(null);
  const [query, setQuery] = useState("");
  const [tab, setTab] = useState("Leads");
  const [stageFilter, setStageFilter] = useState("All stages");
  const [view, setView] = useState("Table");
  const opps = useMemo(() => opportunities.map(opportunityMeta), []);
  const quoteRows = useMemo(() => quotes.map(quoteMeta), []);
  const filteredOpps = opps.filter(item => matchAny(item, query, [row => row.id, row => row.account, row => row.serviceMix]) && (stageFilter === "All stages" || item.stage === stageFilter));
  const filteredQuotes = quoteRows.filter(item => matchAny(item, query, [row => row.id, row => row.account, row => row.productPackage, row => row.opportunityName]));
  const filteredLeads = leads.filter(item => matchAny(item, query, [row => row.id, row => row.account, row => row.product, row => row.stage]));
  const filteredCustomers = customers.filter(item => matchAny(item, query, [row => row.id, row => row.name, row => row.segment, row => row.region]));
  const pipeline = sum(filteredOpps, item => item.tcv);
  const weighted = sum(filteredOpps, item => item.tcv * item.probability / 100);
  function closeModal(message) { setModal(null); if (message) showToast(message); }

  return <><PageHeader title="Sales" description="Telecom CRM workspace for leads, opportunities, custom pricing, approvals, contracts, and account selling motions." />
    <SalesLandingTop query={query} setQuery={setQuery} openModal={setModal} />
    <WorkQueue opportunities={filteredOpps} quotes={filteredQuotes} setRoute={setRoute} />
    <SummaryStrip items={[{ label: "Pipeline", value: formatMoney(pipeline), note: "TCV across open deals" }, { label: "Forecast", value: formatMoney(weighted), note: "Probability adjusted" }, { label: "Open Deals", value: filteredOpps.length, note: "Sales-owned opportunities" }, { label: "At Risk", value: filteredOpps.filter(item => item.pricingRisk !== "Low").length, note: "Pricing or serviceability risk" }]} />
    <div className="sales-crm-toolbar"><label className="inline-search"><Icon name="workflow" className="button-icon" /><select value={stageFilter} onChange={event => setStageFilter(event.target.value)}>{["All stages", ...stages].map(stage => <option key={stage}>{stage}</option>)}</select></label><button className="tiny-button" type="button" onClick={() => setView(view === "Table" ? "Kanban" : "Table")}>{view === "Table" ? "Kanban view" : "Table view"}</button></div>
    <Tabs tabs={salesTabs} active={tab} onChange={setTab} />
    {tab === "Leads" && <Panel title="Leads" description="Lead qualification for telecom accounts and product interest."><DataTable columns={[{ key: "id", label: "Lead" }, { key: "account", label: "Account" }, { key: "source", label: "Source" }, { key: "stage", label: "Stage", render: row => <StatusTag>{row.stage}</StatusTag> }, { key: "product", label: "Product Interest" }, { key: "estValue", label: "Estimated Value", render: row => formatMoney(row.estValue) }, { key: "owner", label: "Owner" }, { key: "actions", label: "Actions", render: row => <div className="table-row-actions"><button className="link-button compact-action" type="button" onClick={() => setRoute(`details/lead/${row.id}`)}>Qualify</button><button className="link-button compact-action" type="button" onClick={() => setModal("opportunity")}>Convert</button></div> }]} rows={filteredLeads} /></Panel>}
    {tab === "Opportunities" && <Panel title="Opportunities" description="Telecom pipeline records connected to accounts, locations, services, pricing, and order conversion.">{view === "Kanban" ? <div className="pipeline-kanban">{stages.map(stage => <div className="kanban-stage" key={stage}><strong>{stage}</strong>{filteredOpps.filter(item => item.stage === stage).map(item => <button className="pipeline-card focus-card" type="button" key={item.id} onClick={() => setRoute(`details/opportunity/${item.id}`)}><span>{item.id} · {item.account}</span><strong>{item.name}</strong><small>{formatMoney(item.estimatedMrc)} MRC · {item.probability}% · {item.nextStep}</small></button>)}</div>)}</div> : <DataTable columns={[{ key: "id", label: "Opportunity" }, { key: "account", label: "Account" }, { key: "serviceMix", label: "Services" }, { key: "locations", label: "Locations" }, { key: "stage", label: "Stage", render: row => <StatusTag tone={row.stage === "Approval" ? "warn" : "blue"}>{row.stage}</StatusTag> }, { key: "estimatedMrc", label: "MRC", render: row => formatMoney(row.estimatedMrc) }, { key: "tcv", label: "TCV", render: row => formatMoney(row.tcv) }, { key: "margin", label: "Margin", render: row => `${row.margin}%` }, { key: "owner", label: "Owner" }, { key: "actions", label: "Actions", render: row => <div className="table-row-actions"><button className="link-button compact-action" type="button" onClick={() => setRoute(`details/opportunity/${row.id}`)}>Open</button><button className="link-button compact-action" type="button" onClick={() => setModal("opportunity")}>Edit</button><button className="link-button compact-action" type="button" onClick={() => { setRoute("orders"); showToast("Order draft opened"); }}>Order</button></div> }]} rows={filteredOpps} />}</Panel>}
    {tab === "Accounts" && <Panel title="Accounts" description="Account-centric CRM view with revenue, region, health, and open sales work."><DataTable columns={[{ key: "id", label: "Account Number" }, { key: "name", label: "Account" }, { key: "segment", label: "Segment" }, { key: "region", label: "Region" }, { key: "services", label: "Active Services", render: row => row.services.join(", ") }, { key: "mrr", label: "MRR", render: row => formatMoney(row.mrr) }, { key: "health", label: "Health" }, { key: "churnRisk", label: "Risk", render: row => <StatusTag tone={row.churnRisk === "High" ? "warn" : "blue"}>{row.churnRisk}</StatusTag> }, { key: "actions", label: "Actions", render: row => <div className="table-row-actions"><button className="link-button compact-action" type="button" onClick={() => setRoute(`details/customer/${row.id}`)}>Customer 360</button><button className="link-button compact-action" type="button" onClick={() => setModal("opportunity")}>New Opp</button></div> }]} rows={filteredCustomers} /></Panel>}
    {tab === "Custom Pricing" && <Panel title="Custom pricing queue" description="Deal desk work for discount exceptions, promo usage, term exceptions, margin review, and competitive responses."><DataTable columns={[{ key: "id", label: "Quote" }, { key: "account", label: "Account" }, { key: "pricingRisk", label: "Risk", render: row => <StatusTag tone={row.pricingRisk === "High" ? "warn" : "blue"}>{row.pricingRisk}</StatusTag> }, { key: "discount", label: "Discount", render: row => `${row.discount}%` }, { key: "margin", label: "Margin", render: row => `${row.margin}%` }, { key: "term", label: "Term", render: row => `${row.term} mo` }, { key: "approvalStatus", label: "Status", render: row => <StatusTag tone={row.approvalRequired ? "warn" : "success"}>{row.approvalStatus}</StatusTag> }, { key: "actions", label: "Actions", render: row => <div className="table-row-actions"><button className="link-button compact-action" type="button" onClick={() => setRoute(`details/quote/${row.id}`)}>Review</button><button className="link-button compact-action" type="button" onClick={() => showToast("Pricing exception routed")}>Route</button></div> }]} rows={filteredQuotes.filter(row => row.customPrice || row.approvalRequired)} /></Panel>}
    {tab === "Approvals" && <Panel title="Approvals" description="Pricing, margin, promo, contract, and quote approval queue."><DataTable columns={[{ key: "id", label: "Quote" }, { key: "account", label: "Account" }, { key: "approvalStatus", label: "Approval" }, { key: "discount", label: "Discount", render: row => `${row.discount}%` }, { key: "margin", label: "Margin", render: row => `${row.margin}%` }, { key: "owner", label: "Owner" }, { key: "actions", label: "Actions", render: row => <div className="table-row-actions"><button className="link-button compact-action" type="button" onClick={() => setRoute(`details/quote/${row.id}`)}>Review</button><button className="link-button compact-action" type="button" onClick={() => showToast("Approval routed to finance")}>Route</button><button className="link-button compact-action" type="button" onClick={() => showToast("Quote approved")}>Approve</button></div> }]} rows={filteredQuotes.filter(row => row.approvalRequired)} /></Panel>}
    {tab === "Contracts" && <Panel title="Contracts" description="Contract term, MSA, order form, ramp, install terms, and renewal tracking."><DataTable columns={[{ key: "id", label: "Contract" }, { key: "account", label: "Account" }, { key: "term", label: "Term" }, { key: "mrc", label: "MRC", render: row => formatMoney(row.mrc) }, { key: "expiration", label: "Expiration" }, { key: "status", label: "Status", render: row => <StatusTag tone={row.approvalRequired ? "warn" : "success"}>{row.approvalRequired ? "Needs Approval" : "Ready"}</StatusTag> }, { key: "actions", label: "Actions", render: row => <button className="link-button compact-action" type="button" onClick={() => setRoute(`details/quote/${row.id}`)}>Open Terms</button> }]} rows={filteredQuotes} /></Panel>}
    {tab === "Sales Enablement" && <section className="sales-crm-grid"><Panel title="Sales Enablement" description="Tools, resources, playbooks, templates, and documentation for sellers and custom pricers."><DataTable columns={[{ key: "resource", label: "Resource" }, { key: "type", label: "Type" }, { key: "audience", label: "Audience" }, { key: "description", label: "Description" }, { key: "actions", label: "", render: row => <button className="link-button compact-action" type="button" onClick={() => showToast(`${row.resource} opened`)}>Open</button> }]} rows={[{ id: "EN-1", resource: "Fiber / DIA seller guide", type: "Product Documentation", audience: "Sales reps", description: "Positioning, qualification, serviceability questions, common objections." }, { id: "EN-2", resource: "Custom pricing guardrails", type: "Pricing Guidance", audience: "Custom pricers", description: "Discount limits, approval thresholds, margin recovery tactics." }, { id: "EN-3", resource: "Competitive battlecards", type: "Battlecard", audience: "Sales managers", description: "Provider positioning, concessions, win/loss notes." }, { id: "EN-4", resource: "Quote templates", type: "Template", audience: "Sales reps", description: "Standard quote language and packaging examples." }]} /></Panel><Panel title="Quick links" description="Knowledge areas separated from the operational Sales workspace."><div className="sales-crm-tool-list"><button type="button" onClick={() => showToast("Tools opened")}>Tools & Utilities<span>Calculators and checklists</span></button><button type="button" onClick={() => showToast("Documentation opened")}>Product Documentation<span>Product and service guides</span></button><button type="button" onClick={() => showToast("Playbooks opened")}>Telecom Playbooks<span>Sales process guidance</span></button><button type="button" onClick={() => showToast("Battlecards opened")}>Competitive Battlecards<span>Competitive positioning</span></button></div></Panel></section>}
    {modal && <Modal title={modal === "lead" ? "Create telecom lead" : "Create telecom opportunity"} onClose={() => setModal(null)} actions={<><button className="button" type="button" onClick={() => closeModal(modal === "lead" ? "Lead created" : "Opportunity saved")}>Save</button><button className="ghost-button" type="button" onClick={() => closeModal("Draft saved")}>Save Draft</button><button className="ghost-button" type="button" onClick={() => setModal(null)}>Cancel</button></>}><SalesModalForm type={modal} /></Modal>}
  </>;
}

function RecordHeader({ breadcrumb, title, status, subtitle, actions, meta }) { return <section className="record-header"><div><div className="breadcrumb">{breadcrumb.join(" / ")}</div><div className="record-title-line"><h2>{title}</h2>{status && <StatusTag tone={["Approval Required", "At Risk", "High"].includes(status) ? "warn" : ["Approved", "Ready", "Active"].includes(status) ? "success" : "blue"}>{status}</StatusTag>}</div>{subtitle && <p>{subtitle}</p>}{meta && <div className="record-meta-row">{meta}</div>}</div><div className="record-actions">{actions}</div></section>; }

export function SalesOpportunityDetail({ id, setRoute, showToast }) {
  const opportunity = opportunityMeta(opportunities.find(item => item.id === id) || opportunities[0]);
  const [tab, setTab] = useState("Summary");
  const quote = quotes.map(quoteMeta).find(item => item.opportunityId === opportunity.id) || quotes.map(quoteMeta)[0];
  const lines = serviceLinesFor(opportunity);
  return <><RecordHeader breadcrumb={["Sales", "Opportunities", opportunity.id]} title={opportunity.name} status={opportunity.status} subtitle={`${opportunity.account} · ${opportunity.accountNumber} · ${opportunity.serviceMix}`} actions={<><ActionButton icon="pricing" variant="button" onClick={() => setRoute(`details/quote/${quote.id}`)}>Open Quote</ActionButton><ActionButton icon="orders" onClick={() => { setRoute("orders"); showToast("Order draft opened from opportunity"); }}>Convert to Order</ActionButton></>} meta={<div className="record-meta-chips"><StatusTag tone="blue">{opportunity.serviceability}</StatusTag><StatusTag tone={opportunity.pricingRisk === "High" ? "warn" : "blue"}>{opportunity.pricingRisk} pricing risk</StatusTag><StatusTag tone="blue">{opportunity.owner}</StatusTag></div>} /><section className="sales-crm-status-board"><div><span>TCV</span><strong>{formatMoney(opportunity.tcv)}</strong></div><div><span>MRC</span><strong>{formatMoney(opportunity.estimatedMrc)}</strong></div><div><span>NRC</span><strong>{formatMoney(opportunity.estimatedNrc)}</strong></div><div><span>Margin</span><strong>{opportunity.margin}%</strong></div></section><Tabs tabs={opportunityTabs} active={tab} onChange={setTab} />{tab === "Summary" && <section className="sales-crm-detail-grid"><Panel title="Opportunity summary" description="Core sales, account, and commercial details."><div className="field-grid"><MiniStat label="Account" value={opportunity.account} note={opportunity.accountNumber} /><MiniStat label="Billing Account" value={opportunity.billingAccount} /><MiniStat label="Segment" value={opportunity.segment} /><MiniStat label="Market" value={opportunity.market} /><MiniStat label="Locations" value={opportunity.locations} /><MiniStat label="Next Step" value={opportunity.nextStep} /></div></Panel><Panel title="Related work" description="Connected quote, serviceability, and order context."><div className="list"><div className="list-item"><div><div className="title">Quote</div><div className="subtitle">{quote.id} · {quote.approvalStatus}</div></div><button className="link-button compact-action" onClick={() => setRoute(`details/quote/${quote.id}`)}>Open</button></div><div className="list-item"><div><div className="title">Order handoff</div><div className="subtitle">Ready after quote approval</div></div><StatusTag tone="blue">Draft</StatusTag></div></div></Panel></section>}{tab === "Serviceability" && <Panel title="Serviceability by location" description="Wireline footprint, wireless coverage, install complexity, and engineering work."><DataTable columns={[{ key: "loc", label: "Location" }, { key: "result", label: "Result" }, { key: "circuits", label: "Circuits" }, { key: "status", label: "Status", render: row => <StatusTag tone={row.status === "Review" ? "warn" : "success"}>{row.status}</StatusTag> }]} rows={[{ id: "L1", loc: "HQ", result: "On-net fiber", circuits: 2, status: "Serviceable" }, { id: "L2", loc: "Branch North", result: "Near-net", circuits: 1, status: "Serviceable" }, { id: "L3", loc: "Warehouse", result: "Engineering review", circuits: 1, status: "Review" }]} /></Panel>}{tab === "Quote Build" && <Panel title="Quote build" description="Products, billing codes, MRC, NRC, cost, and margin by service line."><DataTable columns={[{ key: "product", label: "Product" }, { key: "category", label: "Category" }, { key: "billingCode", label: "Billing Code" }, { key: "mrc", label: "MRC", render: row => formatMoney(row.mrc) }, { key: "nrc", label: "NRC", render: row => formatMoney(row.nrc) }, { key: "cost", label: "Cost", render: row => formatMoney(row.cost) }, { key: "margin", label: "Margin", render: row => `${row.margin}%` }]} rows={lines} /></Panel>}{tab === "Pricing" && <Panel title="Pricing guardrails" description="Approval thresholds and pricing controls."><div className="field-grid"><MiniStat label="Minimum Margin" value="28%" /><MiniStat label="Discount Limit" value="12%" /><MiniStat label="Competitor Pressure" value="Medium" /><MiniStat label="Approval" value={opportunity.pricingRisk === "Low" ? "Not required" : "Required"} /></div></Panel>}{tab === "Approvals" && <Panel title="Approval routing" description="Pricing, margin, discount, promo, and contract approval status."><DataTable columns={[{ key: "step", label: "Step" }, { key: "owner", label: "Owner" }, { key: "status", label: "Status", render: row => <StatusTag tone={row.status === "Approved" ? "success" : row.status === "Pending" ? "warn" : "blue"}>{row.status}</StatusTag> }, { key: "notes", label: "Notes" }]} rows={[{ id: "A1", step: "Pricing review", owner: "Pricing Desk", status: "Approved", notes: "Floor price validated" }, { id: "A2", step: "Sales manager", owner: opportunity.owner, status: "Pending", notes: "Discount exception" }]} /></Panel>}{tab === "Activities" && <Panel title="Activity timeline" description="Discovery, pricing, customer, and serviceability events."><DataTable columns={[{ key: "date", label: "Date" }, { key: "type", label: "Type" }, { key: "owner", label: "Owner" }, { key: "note", label: "Note" }]} rows={activityRows(opportunity.id)} /></Panel>}{tab === "Contract" && <Panel title="Contract terms" description="MSA, order form, renewal, ramp, install terms, and commercial clauses."><div className="field-grid"><MiniStat label="Term" value="36 months" /><MiniStat label="MSA" value="Existing" /><MiniStat label="Ramp" value="90 day" /><MiniStat label="Install Waiver" value="Eligible" /></div></Panel>}{tab === "Order Handoff" && <Panel title="Quote-to-order handoff" description="Operational package for Orders, provisioning, billing, and service activation."><DataTable columns={[{ key: "item", label: "Handoff Item" }, { key: "status", label: "Status", render: row => <StatusTag tone={row.status === "Ready" ? "success" : "warn"}>{row.status}</StatusTag> }, { key: "owner", label: "Owner" }]} rows={[{ id: "H1", item: "Approved quote", status: quote.approvalRequired ? "Pending" : "Ready", owner: "Sales" }, { id: "H2", item: "Serviceability package", status: "Ready", owner: "Network" }, { id: "H3", item: "Billing codes", status: "Ready", owner: "Pricing" }]} /></Panel>}</>;
}

export function SalesQuoteDetail({ id, setRoute, showToast }) {
  const quote = quoteMeta(quotes.find(item => item.id === id) || quotes[0]);
  const opportunity = opportunityMeta(opportunities.find(item => item.id === quote.opportunityId) || opportunities[0]);
  const [tab, setTab] = useState("Quote Summary");
  const lines = serviceLinesFor(opportunity);
  return <><RecordHeader breadcrumb={["Sales", "Quotes", quote.id]} title={`${quote.account} quote`} status={quote.approvalStatus} subtitle={`${quote.productPackage} · ${quote.opportunityName}`} actions={<><ActionButton icon="workflow" variant="button" onClick={() => showToast("Approval routed")}>Submit Approval</ActionButton><ActionButton icon="orders" onClick={() => { setRoute("orders"); showToast("Order draft created"); }}>Convert to Order</ActionButton></>} meta={<div className="record-meta-chips"><StatusTag tone="blue">{quote.term} mo</StatusTag><StatusTag tone={quote.approvalRequired ? "warn" : "success"}>{quote.margin}% margin</StatusTag></div>} /><section className="sales-crm-status-board"><div><span>TCV</span><strong>{formatMoney(quote.tcv)}</strong></div><div><span>MRC</span><strong>{formatMoney(quote.mrc)}</strong></div><div><span>NRC</span><strong>{formatMoney(quote.nrc)}</strong></div><div><span>Discount</span><strong>{quote.discount}%</strong></div></section><Tabs tabs={quoteTabs} active={tab} onChange={setTab} />{tab === "Quote Summary" && <Panel title="Quote summary" description="Commercial quote details, account, opportunity, term, and status."><div className="field-grid"><MiniStat label="Account" value={quote.account} note={quote.accountNumber} /><MiniStat label="Opportunity" value={quote.opportunityName} /><MiniStat label="Package" value={quote.productPackage} /><MiniStat label="Term" value={`${quote.term} months`} /><MiniStat label="Serviceability" value={quote.serviceability} /><MiniStat label="Owner" value={quote.owner} /></div></Panel>}{tab === "Line Items" && <Panel title="Quote line items" description="Product, billing code, MRC, NRC, cost, and margin."><DataTable columns={[{ key: "product", label: "Product" }, { key: "category", label: "Category" }, { key: "billingCode", label: "Billing Code" }, { key: "mrc", label: "MRC", render: row => formatMoney(row.mrc) }, { key: "nrc", label: "NRC", render: row => formatMoney(row.nrc) }, { key: "cost", label: "Cost", render: row => formatMoney(row.cost) }, { key: "margin", label: "Margin", render: row => `${row.margin}%` }]} rows={lines} /></Panel>}{tab === "Pricing Waterfall" && <Panel title="Pricing waterfall" description="List price, costs, promos, discounts, margin, and recommended customer rate."><div className="sales-crm-waterfall"><div><span>List MRC</span><strong>{formatMoney(quote.mrc)}</strong></div><div className="negative"><span>Promo Credit</span><strong>{formatMoney(-Math.round(quote.mrc * 0.06))}</strong></div><div className="negative"><span>Custom Discount</span><strong>{formatMoney(-Math.round(quote.mrc * quote.discount / 100))}</strong></div><div className="final"><span>Customer MRC</span><strong>{formatMoney(Math.round(quote.mrc * (1 - quote.discount / 100)))}</strong></div></div></Panel>}{tab === "Approvals" && <Panel title="Quote approvals" description="Approval route and approval decisions."><DataTable columns={[{ key: "step", label: "Step" }, { key: "status", label: "Status", render: row => <StatusTag tone={row.status === "Approved" ? "success" : "warn"}>{row.status}</StatusTag> }, { key: "owner", label: "Owner" }, { key: "notes", label: "Notes" }]} rows={[{ id: "QA1", step: "Pricing", status: quote.approvalRequired ? "Pending" : "Approved", owner: "Pricing Desk", notes: "Margin guardrail" }, { id: "QA2", step: "Finance", status: quote.approvalRequired ? "Queued" : "Approved", owner: "Finance", notes: "Discount review" }]} /></Panel>}{tab === "PDF Preview" && <Panel title="Quote PDF preview" description="Customer-facing telecom quote structure."><div className="sales-crm-quote-document"><DataTable columns={[{ key: "product", label: "Product" }, { key: "mrc", label: "Monthly", render: row => formatMoney(row.mrc) }, { key: "nrc", label: "One-time", render: row => formatMoney(row.nrc) }, { key: "serviceability", label: "Serviceability" }]} rows={lines} /></div></Panel>}{tab === "Audit" && <Panel title="Quote audit" description="Quote changes, approvals, pricing edits, and customer delivery events."><DataTable columns={[{ key: "date", label: "Date" }, { key: "type", label: "Type" }, { key: "owner", label: "Owner" }, { key: "note", label: "Note" }]} rows={activityRows(quote.id)} /></Panel>}</>;
}

export function SalesLeadDetail({ id, showToast }) {
  const lead = leads.find(item => item.id === id) || leads[0];
  const [tab, setTab] = useState("Qualification");
  return <><RecordHeader breadcrumb={["Sales", "Leads", lead.id]} title={lead.account} status={lead.stage} subtitle={`${lead.source} · ${lead.product}`} actions={<><ActionButton icon="opportunities" variant="button" onClick={() => showToast("Lead converted to opportunity")}>Convert</ActionButton><ActionButton icon="workflow" onClick={() => showToast("Activity logged")}>Log Activity</ActionButton></>} /><Tabs tabs={leadTabs} active={tab} onChange={setTab} />{tab === "Qualification" && <Panel title="Lead qualification" description="Telecom-specific qualification for product fit, budget, authority, timing, and location scope."><div className="field-grid"><MiniStat label="Product Interest" value={lead.product} /><MiniStat label="Estimated Value" value={formatMoney(lead.estValue)} /><MiniStat label="Source" value={lead.source} /><MiniStat label="Owner" value={lead.owner} /><MiniStat label="Need" value="Connectivity / managed services" /><MiniStat label="Timing" value="30-90 days" /></div></Panel>}{tab === "Account Fit" && <Panel title="Account fit" description="Segment, region, serviceability, and potential services."><DataTable columns={[{ key: "area", label: "Area" }, { key: "result", label: "Result" }, { key: "notes", label: "Notes" }]} rows={[{ id: "F1", area: "Wireline", result: "Potential", notes: "Footprint review required" }, { id: "F2", area: "Wireless", result: "Strong", notes: "Coverage available" }, { id: "F3", area: "Managed Services", result: "Good fit", notes: "Router and SD-WAN attach" }]} /></Panel>}{tab === "Conversion Plan" && <Panel title="Conversion plan" description="Steps to convert lead into opportunity and quote."><DataTable columns={[{ key: "step", label: "Step" }, { key: "owner", label: "Owner" }, { key: "status", label: "Status" }]} rows={[{ id: "C1", step: "Confirm account and locations", owner: lead.owner, status: "Open" }, { id: "C2", step: "Run serviceability", owner: "Sales Engineering", status: "Queued" }, { id: "C3", step: "Create opportunity", owner: lead.owner, status: "Next" }]} /></Panel>}{tab === "Activity" && <Panel title="Lead activity" description="Lead touchpoints and qualification notes."><DataTable columns={[{ key: "date", label: "Date" }, { key: "type", label: "Type" }, { key: "owner", label: "Owner" }, { key: "note", label: "Note" }]} rows={activityRows(lead.id)} /></Panel>}</>;
}
